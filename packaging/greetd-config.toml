# A sample /etc/greetd/config.toml. Copy it by hand — the installer does not
# touch system files.

[terminal]
vt = 1

[default_session]
command = "Hyprland -c /usr/share/my-greeter/hyprland.conf"
user = "greeter"
